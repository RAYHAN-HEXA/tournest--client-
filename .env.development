# Dev overrides (used by `vite` only, not by `vite build`).
# Empty VITE_API_URL -> requests hit the Vite dev-server proxy (/api -> tournest-server),
# keeping them same-origin so CORS and cached cross-origin preflights never interfere.
VITE_API_URL=
